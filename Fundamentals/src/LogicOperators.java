public class LogicOperators {
    public static void main(String[] args) {
        boolean resp;

        resp = true && true; // true
        resp = true || false; // true
        resp = !true; // false
    }
}
